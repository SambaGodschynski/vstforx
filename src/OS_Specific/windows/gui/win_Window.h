#ifndef WIN_WINDOWS_H
#define WIN_WINDOWS_H

#include "vstgui.h"
#include "OS_Specific/WindowDef.h"
#include <map>
#include <string>
using namespace std;

namespace ppiGui {
//============================================================================================================
IWindow::Ptr createWindow ( const VSTGUI::CRect &pos );
//============================================================================================================
// Klasse: Window
//============================================================================================================
class Window : public IWindow {
friend Ptr createWindow ( const VSTGUI::CRect &size, const IWindow::WindowFlags &flags, void* );
private:
	//--------------------------------------------------------------------------------------------------------
	static WNDCLASSEX wndClass;
	//--------------------------------------------------------------------------------------------------------
	HWND hwnd;
	//--------------------------------------------------------------------------------------------------------
	bool posChanging;
	//--------------------------------------------------------------------------------------------------------
	static int instances;
	//--------------------------------------------------------------------------------------------------------
	static LRESULT CALLBACK osCallback(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam);
	//--------------------------------------------------------------------------------------------------------
	static map < HWND, Ptr > hwnd2Object; // fuer WND_MSG zuordnung
	//--------------------------------------------------------------------------------------------------------
	void destroyWindow();
	//--------------------------------------------------------------------------------------------------------
	UINT createWinStyleFlags();
protected:
	//--------------------------------------------------------------------------------------------------------
	string caption;
	//--------------------------------------------------------------------------------------------------------
	void messageFromOs ( UINT message, WPARAM wp, LPARAM lp );
	//--------------------------------------------------------------------------------------------------------
	Window ( const VSTGUI::CRect &size, const WindowFlags &flags, void *parentHandle = NULL );
public:
	//--------------------------------------------------------------------------------------------------------
	void setCaption ( const string &caption ) { 
		this->caption = caption; 
		if (hwnd) SetWindowText ( hwnd, caption.c_str() ); 
	} 
	//--------------------------------------------------------------------------------------------------------
	virtual void showWindow( const CPoint &p = CPoint() );
	//--------------------------------------------------------------------------------------------------------
	virtual void closeWindow();
	//--------------------------------------------------------------------------------------------------------
	virtual bool isOpen() const { return hwnd!=NULL; }
	//--------------------------------------------------------------------------------------------------------
	virtual void setPos ( const CPoint &pos );
	//--------------------------------------------------------------------------------------------------------
	virtual CPoint getPos() const;
	//--------------------------------------------------------------------------------------------------------
	virtual void setSize ( const VSTGUI::CRect& );
	//--------------------------------------------------------------------------------------------------------
	virtual void * getHandle() const { return hwnd; }
	//--------------------------------------------------------------------------------------------------------
	virtual ~Window();
};
//============================================================================================================
// Klasse : CDrawContextPlus
//============================================================================================================
class CDrawContextPlus {
private:
	//--------------------------------------------------------------------------------------------------------
	CPoint cursor;
	//--------------------------------------------------------------------------------------------------------
	CDrawContext &cD;
public:
	//--------------------------------------------------------------------------------------------------------
	CDrawContextPlus ( CDrawContext &c );
	//--------------------------------------------------------------------------------------------------------
	void moveTo ( const CPoint &p ) { cursor = p; }
	//--------------------------------------------------------------------------------------------------------
	void lineTo ( const CPoint &p ) { drawLine (cursor, p); }
	//--------------------------------------------------------------------------------------------------------
	virtual void drawLine ( const CPoint &p1, const CPoint &p2 );
	//--------------------------------------------------------------------------------------------------------
	virtual void drawRect ( const VSTGUI::CRect &r );
	//--------------------------------------------------------------------------------------------------------
	virtual void fillRect ( const VSTGUI::CRect &r );
	//--------------------------------------------------------------------------------------------------------
	virtual void drawArc ( const VSTGUI::CRect &r, const CPoint &p, const CPoint &p2 );
	//--------------------------------------------------------------------------------------------------------
	virtual void fillArc ( const VSTGUI::CRect &r, const CPoint &p, const CPoint &p2 );
	//--------------------------------------------------------------------------------------------------------
	virtual void setFrameColor ( const CColor &col ) {  cD.setFrameColor(col); }
	//--------------------------------------------------------------------------------------------------------
	virtual void setFillColor ( const CColor &col ) { cD.setFillColor(col); }
	//--------------------------------------------------------------------------------------------------------
	virtual void setFontColor ( const CColor &col ) { cD.setFontColor(col); }
	//--------------------------------------------------------------------------------------------------------
	virtual void setFont ( CFont fontId, const long size = 0, long style = 0 ) { 
		cD.setFont(fontId, size, style); 
	}
	//--------------------------------------------------------------------------------------------------------
	virtual void setLineStyle ( CLineStyle style ) { cD.setLineStyle(style); }
	//--------------------------------------------------------------------------------------------------------
	virtual void setLineWidth ( CCoord w ) { cD.setLineWidth(w); }
	//--------------------------------------------------------------------------------------------------------
	virtual void drawString ( const char* str, const VSTGUI::CRect &size, const short opaque=false,
							  const CHoriTxtAlign hAlign=kCenterText ) 
	{
		cD.drawString(str, size, opaque, hAlign);
	}
	//--------------------------------------------------------------------------------------------------------
	~CDrawContextPlus();
};
} // namespace ppiGui

#endif