#ifndef PPIEDITOR_H
#define PPIEDITOR_H


#include <vector>
#include "com/one4All.h"
#include "vstgui.h"
#include "CircuidView.h"
#include "CircuidControl.h"
#include "VSTPlugView.h"
#include "IdleListener.h"
#include <list>
#include "ViewEvents.h"
#include <boost/unordered_map.hpp>
#include "MainCtrl.h"
#include "com/SystemCommand.h"
#include "com/PluginCollection.h"
#include "gui/Resources.h"
#include "processing/Graph.h"

using namespace std;

namespace ppiGui{
class PpiEditor;
//============================================================================================================
//	Klasse OwnIdleTimer:
//============================================================================================================
class OwnIdleTimer : public com::SysTimer {
private:
	//--------------------------------------------------------------------------------------------------------
	PpiEditor *ed;
protected:
	//--------------------------------------------------------------------------------------------------------
	virtual void callBack();
public:
	//--------------------------------------------------------------------------------------------------------
	OwnIdleTimer( PpiEditor *ed );
};
//============================================================================================================
//  Relation View=>Graph :
//============================================================================================================
typedef boost::unordered_map< CircuidView*, Graph::Ptr > View2Graph;
Graph::Ptr getRelatedGraph ( CircuidView* );
//============================================================================================================
//	Klasse PpiEditor:
//  Haupt PPI GUI Klasse. Generalisiert AeffGUIEditor von vstgui.
//============================================================================================================
class PpiEditor : 
	public AEffGUIEditor, 
    private CommandExecuter,
	public EventSender<OnIdle>, 
	public EventListener<OnResize>,
	public EventListener<ScanInterrupted>
{
public:
	//--------------------------------------------------------------------------------------------------------
	typedef list< processing::PluginInfo > ScanResults;
private:
	//--------------------------------------------------------------------------------------------------------
	com::CommandWorker::Ptr sysExec;
	//--------------------------------------------------------------------------------------------------------
	OwnIdleTimer *idleTimer;
	//--------------------------------------------------------------------------------------------------------
	ViewRelations view2model;
	//--------------------------------------------------------------------------------------------------------
	Settings::Ptr settings;
	//--------------------------------------------------------------------------------------------------------
	MainCtrl::Ptr mainCtrl;
	//--------------------------------------------------------------------------------------------------------
	Mutex mutex;
	//--------------------------------------------------------------------------------------------------------
	CircuidView *circuidView;
	//--------------------------------------------------------------------------------------------------------
	CircuidControl *circuidControl;
	//--------------------------------------------------------------------------------------------------------
	ScanResults scanResults; // wird waehrend scan gefuellt und von scanDlg wieder entfernt
	                             // zweck: zwischen sp. falls close() waehrend scan.
	//--------------------------------------------------------------------------------------------------------
	processing::Graph::Ptr graph;
	//--------------------------------------------------------------------------------------------------------
	static int instances;
	//--------------------------------------------------------------------------------------------------------
	stringstream viewbuffer; // view objekte persistiert
	//--------------------------------------------------------------------------------------------------------
	bool firstInit, scanInterrupted;
	//--------------------------------------------------------------------------------------------------------
	void saveView(); 
	//--------------------------------------------------------------------------------------------------------
	void loadView(); 
	//--------------------------------------------------------------------------------------------------------
	void releaseView();
protected:
	//--------------------------------------------------------------------------------------------------------
	virtual bool open (void *ptr);
	//--------------------------------------------------------------------------------------------------------
	virtual void close ();
public:
	//--------------------------------------------------------------------------------------------------------
	// wird von CommandWorker ausgefuehrt
	void addExtraTimerCmd ( Command::Ptr sysCmd ) {
		sysExec->addCommand ( sysCmd );
	}
	//--------------------------------------------------------------------------------------------------------
	virtual void addCommand ( Command::Ptr cmd, Priority priority = NORMAL ) { 
		CommandExecuter::addCommand ( cmd, priority );
	}
	//--------------------------------------------------------------------------------------------------------
	MainCtrl * getMainCtrl() { return static_cast<MainCtrl*>( frame ); } 
	//--------------------------------------------------------------------------------------------------------
	void save ( oArchive &ar );
	//--------------------------------------------------------------------------------------------------------
	void * getSystemWindow() { return systemWindow; }
	//--------------------------------------------------------------------------------------------------------
	void load ( iArchive &ar, processing::Graph::Ptr &graph ); 
	//--------------------------------------------------------------------------------------------------------
	void setGraph( const processing::Graph::Ptr &ptr ) { graph = ptr; }
	//--------------------------------------------------------------------------------------------------------
	void eventHandler ( void *src, const OnResize &ev );
	//--------------------------------------------------------------------------------------------------------
	void eventHandler ( void *src, const ScanInterrupted &ev );
	//--------------------------------------------------------------------------------------------------------
	processing::Graph::Ptr getGraph() { return graph; }
	//--------------------------------------------------------------------------------------------------------
	PpiEditor( void *ptr );
	//--------------------------------------------------------------------------------------------------------
	virtual ~PpiEditor();
	//--------------------------------------------------------------------------------------------------------
	void idle();
};

} //namespace ppiGui

#endif


